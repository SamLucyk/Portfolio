<!-- views/partials/footer.ejs -->
<span id="footer-container">
    <div id="footer" class="footer">
        <div id="sketch-holder" class="center">
              <!-- Our sketch will go here! -->
        </div>
        <div class="footer-links">
            <a id="project-link">Projects</a> · 
            <a href="https://www.linkedin.com/in/sam-lucyk-602a3555/" target="_blank">LinkedIn</a> · 
            <a href="https://github.com/SamLucyk" target="_blank">GitHub</a>
        </div>
        <p class="disable-select reverse-shine text-center text-muted">Sam Lucyk 2017</p>
    </div>
</span>
</div>
<footer>
    
    
</footer>
    
<script>
    $( ".nav-item" ).click(function() {
      console.log('clicked');
      setActive($(this).attr('page'), true);
    });
        
    $('#project-link').click(function() {
        setActive('projects');
        
    });
        
    $('.shine').each(
        function () {
            //Extract text from html, and attach it to "Txt" variable.
            var Txt = $(this).text();
            //Empty var-string waiting for loop.
            var Gtxt = '';
            //Loop through text to add <span id="green> on every letter.
            for (i in Txt) {
                //add letter by letter to Gtxt ( <span id="green"> letter </span> )
                Gtxt = (Gtxt + '<span class="hover-letter">' + Txt[i] + '</span>');

            }
            //Add processed text to Html $('h2')
            $(this).html(Gtxt);
        });
 
    $('.reverse-shine').each(
        function () {
            //Extract text from html, and attach it to "Txt" variable.
            var Txt = $(this).text();
            //Empty var-string waiting for loop.
            var Gtxt = '';
            //Loop through text to add <span id="green> on every letter.
            for (i in Txt) {
                //add letter by letter to Gtxt ( <span id="green"> letter </span> )
                Gtxt = (Gtxt + '<span class="reverse-hover-letter">' + Txt[i] + '</span>');

            }
            //Add processed text to Html $('h2')
            $(this).html(Gtxt);
        });
    
    function getRandomInt(min, max) {
        min = Math.ceil(min);
        max = Math.floor(max);
        return Math.floor(Math.random() * (max - min)) + min; //The maximum is exclusive and the minimum is inclusive
    }
</script>
</body>
</html>
